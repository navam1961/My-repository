// Program to calculate area of a square whose side is given
#include<stdio.h>
int main() {
    float a;
    printf("Enter side of square: ");
    scanf("%f",&a);
    printf("Area of square is %f",a*a);
    return 0;
}